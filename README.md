    1  apt get update
    2  apt get-update
    3  apt update
    4  apt install docker.io
    5  usermod -aG docker $USER
    6  docker ps
    7  clear
    8  git clone https://github.com/vpnsoni426/Three-Tier-docker-compose-app.git .
    9  git clone https://github.com/vpnsoni426/Three-Tier-docker-compose-app.git
   10  ls -l
   11  apt install -y docker-compose-plugin
   12  apt-get update
   13  apt-get install docker-compose-plugin
   14  clear
   15  docker compose version
   16  DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker}
   17  mkdir -p $DOCKER_CONFIG/cli-plugins
   18  curl -SL https://github.com/docker/compose/releases/download/v5.5.0/docker-compose-linux-x86_64 -o $DOCKER_CONFIG/cli-plugins/docker-compose
   19  ls -l
   20  chmod +x $DOCKER_CONFIG/cli-plugins/docker-compose
   21  sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
   22  docker compose version
   23  clear
   24  ls 
   25  cd Three-Tier-docker-compose-app  
   26  ls -l
   27  cd three-tier-backend
   28  ls -l
   29  cat Dockerfile
   30  pwd
   31  cd ..
   32  ls -l
   33  cd three-tier-frontend
   34  ls -l
   35  cat Dockerfile
  
   42  ls -l
   43  cd ..
   44  ls -l
   45  vi docker-compose.yml
   46  cat docker-compose.yml
   47  rm -r docker-compose.yml
   48  ls -l
   49  vi docker-compose.yml
   50  ls -l
   51  cat docker-compose.yml
   52  vi init.sql
   53  ls -l
   54  vi three-tier-backend/.env
   55  vi three-tier-frontend/.env
   56  docker compose build
   57  docker compose up -d
   58  ls -l
   59  cd three-tier-backend
   60  ls -l
   61  vi .env
   62 
   64  ls -la
   65  docker compose config
   66  cd ..
   67    69  
   70  rm -r docker-compose.yml
   71  ls -la
   72  vi docker-compose.yml
   73  docker compose up -d
  

   allow http port aws and copy the public ip of ubuntu and hit into the browser
